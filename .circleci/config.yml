version: 2.1

executors:
  java-executor:
    docker:
      - image: cimg/openjdk:21.0.4
    working_directory: ~/project

workflows:
  version: 2
  build-and-test:
    jobs:
      - build
      - dependency-check:
          requires:
            - build
      - build-dist:
          requires:
            - build

  release:
    jobs:
      - release:
          filters:
            tags:
              only: /^v.*/
            branches:
              ignore: /.*/

jobs:
  build:
    executor: java-executor
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: Clear Gradle Cache
          command: rm -rf ~/.gradle/caches/8.5
      - restore_cache:
          keys:
            - v1-gradle-{{ checksum "build.gradle" }}
            - v1-gradle-
      - run:
          name: Build
          command: export GRADLE_OPTS="--enable-native-access=ALL-UNNAMED" && ./gradlew build
      - run:
          name: Run Tests
          command: export GRADLE_OPTS="--enable-native-access=ALL-UNNAMED" && ./gradlew test
      - save_cache:
          key: v1-gradle-{{ checksum "build.gradle" }}
          paths:
            - ~/.gradle/caches
            - ~/.gradle/wrapper
      - store_test_results:
          path: build/test-results/
      - store_artifacts:
          path: build/
          destination: build

  dependency-check:
    executor: java-executor
    context: 1904a592-1c0b-4082-8d3e-8b3028ab11e1
    steps:
      - checkout
      - restore_cache:
          keys:
            - v1-gradle-deps-{{ checksum "build.gradle" }}
            - v1-gradle-deps-
      - run:
          name: Run OWASP Dependency Check
          command: export GRADLE_OPTS="--enable-native-access=ALL-UNNAMED" && ./gradlew dependencyCheckAnalyze
      - save_cache:
          key: v1-gradle-deps-{{ checksum "build.gradle" }}
          paths:
            - ~/.gradle/caches
      - store_artifacts:
          path: build/reports/dependency-check-report.html
          destination: security-report

  build-dist:
    executor: java-executor
    steps:
      - checkout
      - restore_cache:
          keys:
            - v1-gradle-dist-{{ checksum "build.gradle" }}
            - v1-gradle-dist-
      - run:
          name: Build Distribution
          command: export GRADLE_OPTS="--enable-native-access=ALL-UNNAMED" && ./gradlew installDist
      - save_cache:
          key: v1-gradle-dist-{{ checksum "build.gradle" }}
          paths:
            - ~/.gradle/caches
      - store_artifacts:
          path: build/install/
          destination: distribution

  release:
    executor: java-executor
    steps:
      - checkout
      - restore_cache:
          keys:
            - v1-gradle-release-{{ checksum "build.gradle" }}
            - v1-gradle-release-
      - run:
          name: Build Distribution
          command: export GRADLE_OPTS="--enable-native-access=ALL-UNNAMED" && ./gradlew installDist fatJar
      - save_cache:
          key: v1-gradle-release-{{ checksum "build.gradle" }}
          paths:
            - ~/.gradle/caches
      - run:
          name: Create GitHub Release
          command: |
            if [ -n "$GITHUB_TOKEN" ]; then
              curl -L https://github.com/softprops/action-gh-release/releases/latest/download/action-gh-release-linux-linux-amd64.tar.gz | tar -xz
              ./dist/release
              mv build/libs/*.jar .
              mv build/install/flexo/bin/flexo .
              mv build/install/flexo/bin/flexo.bat .
              ./action-gh-release --files "*.jar,flexo,flexo.bat" --generate-release-notes
            fi
          environment:
            GITHUB_TOKEN: ${GITHUB_TOKEN}
