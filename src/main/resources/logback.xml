<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Console appender -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Root logger -->
    <root level="WARN">
        <appender-ref ref="STDOUT" />
    </root>

    <!-- Flexo CLI logging -->
    <logger name="org.openmbee.flexo.cli" level="DEBUG" />
    <logger name="org.openmbee.flexo.cli.client.AuthenticationHandler" level="DEBUG" />

    <!-- Apache HTTP Client logging (set to WARN to reduce noise) -->
    <logger name="org.apache.hc" level="WARN" />
    <logger name="org.apache.http" level="WARN" />

    <!-- Jena logging (set to WARN to reduce noise) -->
    <logger name="org.apache.jena" level="WARN" />

</configuration>
